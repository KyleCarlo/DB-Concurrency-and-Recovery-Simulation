<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DB Logging and Recovery</title>
    <link rel="stylesheet" href="./styles/index.css">
</head>
<body>
    <form action="/logNrecover" method="post" style="display: none;">
        <input type="submit" value="submit">
    </form>
    <h1>Replication Logs</h1>
    <table>
        <thead>
            <tr>
                <th>Log ID</th><th>Timestamp</th><th>Source Node</th><th>Target Node</th><th>Action</th>
            </tr>
        </thead>
        <tbody>
            
            <% for (let index = recoveryLogs.length-1; index > -1; index--) {%>
                <tr>
                    <td><%=recoveryLogs[index].logId%></td>
                    <td><%=recoveryLogs[index].timestamp%></td>
                    <td><%=recoveryLogs[index].source%></td>
                    <td><%=recoveryLogs[index].target%></td>
                    <td><%=recoveryLogs[index].action%></td>
                </tr>
            <% } %> 
        </tbody>
    </table>
</body>
<script>
    var submit = document.querySelector('input[type="submit"]');
    setTimeout(() => {
        submit.click();
    }, 1250);
</script>
</html>